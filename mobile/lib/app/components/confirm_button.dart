import 'package:flutter/material.dart';

class ComfirmButton extends StatelessWidget {
  const ComfirmButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("ComfirmButton");
  }
}
