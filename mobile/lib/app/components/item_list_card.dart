import 'package:flutter/material.dart';

class ItemListCard extends StatelessWidget {
  const ItemListCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("ItemListCard");
  }
}
